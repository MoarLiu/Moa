: "${APP_VERSION:=1.2.0}"
: "${APP_BUILD:=116}"
: "${APP_RELEASE_VERSION:=1.2.0-rc.1}"
: "${APP_ARCH:=$(uname -m)}"
: "${MACOS_DEPLOYMENT_TARGET:=12.0}"
: "${SWIFTC_TARGET:=${APP_ARCH}-apple-macosx${MACOS_DEPLOYMENT_TARGET}}"
